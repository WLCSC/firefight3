<p id="notice"><%= notice %></p>

<p>
  <strong>User sid:</strong>
  <%= @subscription.user_sid %>
</p>

<p>
  <strong>Subscribable:</strong>
  <%= @subscription.subscribable %>
</p>

<%= link_to 'Edit', edit_subscription_path(@subscription) %> |
<%= link_to 'Back', subscriptions_path %>
